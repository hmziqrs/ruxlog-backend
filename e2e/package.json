{
  "name": "ruxlog-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright browser e2e scaffold for ruxlog (issue #33). Currently drives a real Chromium against API-served responses produced by the e2e workflow; once the dioxus web build is available in CI, specs will additionally target the consumer UI.",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
